import DocuClicked from '../../ic_docu-Clicked.svg';
import DocuDefault from '../../ic_docu-Default.svg';
import DocuHover from '../../ic_docu-Hover.svg';

export const registry = {
  clicked: DocuClicked,
  default: DocuDefault,
  hover: DocuHover,
};

export type Variant = keyof typeof registry;
import DocuClicked from '../../ic_docu-Clicked.svg';
import DocuDefault from '../../ic_docu-Default.svg';
import DocuHover from '../../ic_docu-Hover.svg';

export const registry = {
  clicked: DocuClicked,
  default: DocuDefault,
  hover: DocuHover,
};

export type Variant = keyof typeof registry;
